<?php 

/**
 * These are all samples. They can be customized to your needs and linking.
 * For example the sidebar could use cusom markup, the menus and wp featured extented and so on.
 */


add_theme_support( 'post-thumbnails' ); 
add_theme_support( 'post-formats', array( 'aside', 'gallery' ) );
register_nav_menu('main_menu', 'Main Menu');
register_sidebar();